## Лабораторная работа №11: Контейнеризация мультиязычных приложений

**Автор:** Авдошина Ангелина Евгеньевна 
**Группа:** 221331
**Вариант:** 8

## PROMPT_LOG

## Этап 1: Go-сервис

### Промпт 1
**Промпт:** "Разработай падающие тесты для Go-сервиса. Нужно проверить эндпоинт /health (JSON-ответ, статус 200) и работу с переменной окружения PORT (по умолчанию 8080)."

**Результат:** 
- Создан `main_test.go` с тестами `TestHealthEndpoint`, `TestPortEnvVar`, `TestDefaultPort`.
- Выполнение go test -v завершилось с FAIL.

### Промпт 2
**Промпт:** "Напиши минимальную реализацию Go-сервера, чтобы тесты стали зелёными. Требуются обработчики /health (JSON {"status":"healthy"}) и / ("OK"), а также чтение PORT из окружения."

**Результат:**
- Создан main.go с функцией setupRouter() и хендлерами.
- Тесты успешно пройдены.

### Промпт 3
**Промпт:** "Создай многоступенчатый Dockerfile для Go: первый этап — golang:1.22-alpine для сборки, финальный образ — scratch. Отключи CGO, перенеси только бинарный файл. Добавь HEALTHCHECK, вызывающий /server healthcheck, и пробрось PORT."

**Результат:**
- Готов Dockerfile (multi-stage: golang:1.22-alpine → scratch).
- Установлен CGO_ENABLED=0, использованы флаги -ldflags="-w -s".
- В код добавлена поддержка аргумента healthcheck.
- Итоговый размер образа: 7.76MB.

## Этап 2: Python-сервис

### Промпт 4
**Промпт:** "Напиши падающие тесты для Python-сервиса на FastAPI: проверка /health, / и чтение PORT из окружения."

**Результат:**
- Создан test_app.py с использованием pytest и httpx.

### Промпт 5
**Промпт:** "Реализуй FastAPI-приложение, которое проходит все тесты. Добавь эндпоинты /health (JSON), / ("OK"), поддержку PORT и DEBUG."

**Результат:**
- Написан app.py на FastAPI под управлением uvicorn.
- Тесты успешно пройдены.

### Промпт 6
**Промпт:** "Подготовь Dockerfile для Python с оптимизацией: используй slim-версию образа, многоэтапную сборку для кэширования зависимостей, HEALTHCHECK через curl или встроенный Python-модуль."

**Результат:**
- Создан Dockerfile на базе python:3.14-slim.
- HEALTHCHECK выполняется через python -c "import urllib.request...".
- Размер образа: 207MB.

## Этап 3: Docker Compose (Go + Python)

### Промпт 7
**Промпт:** "Создай docker-compose.yml, который объединяет Go и Python сервисы. Добавь общую сеть, проброс портов через .env, используй healthcheck из Dockerfile."

**Результат:**
- Появился docker-compose.yml с сервисами go-service, python-service.
- Добавлен .env.example с переменными GO_PORT, PYTHON_PORT, DEBUG.

## Этап 4: Повышенная сложность (Rust + Watchtower)

### Промпт 8
**Промпт:** "Собери Rust-приложение на actix-web. Требуется полностью статическая сборка с musl. Финальный слой — alpine (нужен для curl в HEALTHCHECK). Добейся минимального размера образа."

**Результат:**
- Создана структура rust-service с Cargo.toml и src/main.rs.
- Dockerfile: rust:1.85-alpine → сборка под x86_64-unknown-linux-musl → финальный alpine:latest.
- HEALTHCHECK через curl -f http://localhost:$PORT/health.
- Итоговый размер: 3.76MB.

### Промпт 9
**Промпт:** "Добавь в docker-compose.yml сервис watchtower, чтобы контейнеры обновлялись автоматически. Установи интервал проверки 60 секунд и автоочистку старых образов. Включи rust-service в оркестрацию."

**Результат:**
- Обновлён docker-compose.yml: появились rust-service и watchtower.
- Для совместимости указана DOCKER_API_VERSION=1.40.

### Промпт 10
**Промпт:** "Пропиши переменные окружения для Rust-сервиса в .env и .env.example."

**Результат:**
- Добавлена RUST_PORT=8081.

## Итоги
- Инструмент: Cursor
- Общее количество промптов: 10
- Что пришлось исправлять вручную: ничего
- Время: ~120 мин
